import { Decorator } from '@storybook/react';
// eslint-disable-next-line pathckeck-plugin/layer-imports
import '@/app/styles/index.scss';

export const StyleDecorator: Decorator = (Story) => Story();
